import Header from "../../components/Header";
import Title from "../../components/Title";

const Contact = () => {
  return (
    <Header id="Connect">
      <Title id="Connect" page={5} />
    </Header>
  );
};

export default Contact;
